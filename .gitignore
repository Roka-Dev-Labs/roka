# Python
.venv/
.venv-video/
__pycache__/
*.pyc
.pytest_cache/

# Rust — source and build (private; only binaries are distributed)
src/
Cargo.toml
Cargo.lock
target/

# Node
node_modules/

# Environment & secrets
*.env
.env*
.npmrc
**/.npmrc

# OS
.DS_Store
Thumbs.db

# IDE
.cursor/
.vscode/
*.swp

# Infrastructure / devops (never commit)
devops/
scripts/

# Private backend (never commit)
backend/
api/
vercel-frontend.json
rust-toolchain.toml

# Local test logs (LogHub samples)
Testing/
*.log

# Build artifacts & logs
*.gz
real_logs.txt
test_*.txt
Anthropic-Cybersecurity-Skills/

# Large feature demo videos (GitHub 100MB limit) — keep compressed files under media/
Log Compaction Video.mp4
*.mp4
!media/
!media/*.mp4
!media/.gitkeep

# Private product website (lives in private repo — local backup only)
.private-product/

# Staging copies of other public repos (push separately)
public-repos/
.vercel

# Duplicate Aider skill copies
.aider-desk/
